name: Regression tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: [3.9]

    steps:
    - uses: actions/checkout@v2
    - name: Setup Python ${{ matrix.python-version }}
      uses: actions/setup-python@v2
      with:
        python-version: ${{ matrix.python-version }}
    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install behave coveralls coverage
        python setup.py develop
    - name: Setup upterm session
      uses: lhotari/action-upterm@v1
    - name: Run tests
      run: |
        behave --logging-level DEBUG --no-capture --no-capture-stderr --no-logcapture --no-color
#      run: |
#        coverage run --source cicada -m behave --logging-level INFO --no-capture --no-capture-stderr --no-logcapture --no-color
#        coverage combine
#        coverage report
    - name: Upload coverage to Coveralls
      run: coveralls --service=github
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
